{
  "version": 2,
  "buildCommand": "bun install && bun run build",
  "installCommand": "bun install",
  "builds": [
    {
      "src": "dist/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/server.js"
    }
  ]
}
